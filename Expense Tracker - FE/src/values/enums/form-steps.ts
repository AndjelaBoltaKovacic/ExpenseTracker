export enum AddTransactionSteps {
  Add,
  Confirm,
  Success,
  Fail,
}

export enum EditTransactionSteps {
  Edit,
  Confirm,
  Success,
  Fail,
}

export enum DeleteTransactionSteps {
  Confirm,
  Success,
  Fail,
}

export enum AddReminderSteps {
  Add,
  Success,
  Fail,
}

export enum Outcome {
  Success = 'success',
  Fail = 'fail',
}